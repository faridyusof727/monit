{
  "scripts": {
    "dokku": {
      "predeploy": "echo ${FIREBASE} > /app/firebase-admin-sdk.json && mkdir -p store/counter && echo '{\"Current\":1,\"Max\":2000}' > /app/store/counter/counter.json"
    }
  }
}
