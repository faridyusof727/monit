{
  "scripts": {
    "dokku": {
      "predeploy": "echo ${FIREBASE} > /app/firebase-admin-sdk.json && mkdir store/counter && echo '{\"Current\":1,\"Max\":2000}' > /app/store/counter/counter.json"
    }
  }
}
